
DATABASE_URL=mssql+pyodbc:///?odbc_connect=Driver=ODBC Driver 17 for SQL Server;Server=localhost;Database=MyDiaryDB;Trusted_Connection=yes;

MAIL_DEFAULT_SENDER=kevohmutwiri35@gmail.com

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=kevohmutwiri35@gmail.com
MAIL_PASSWORD=gwgr beby abhu edvo

# Security Settings
PASSWORD_RESET_SALT=your-password-reset-salt
PASSWORD_RESET_TOKEN_MAX_AGE=3600

# Database Configuration
# Option 1: SQL Server with Trusted Connection (Recommended)
DATABASE_URL=mssql+pyodbc:///?odbc_connect=Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=MyDiaryDB;Trusted_Connection=yes;

# Option 2: SQL Server with username/password
# DATABASE_URL=mssql+pyodbc://username:password@server/MyDiaryDB?driver=ODBC+Driver+17+for+SQL+Server

# Option 3: PostgreSQL
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Option 4: SQLite (for development)
# DATABASE_URL=sqlite:///my_diary.db
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# Monitoring and Analytics
SENTRY_DSN=your-sentry-dsn-here
GOOGLE_ANALYTICS_ID=G-QLGGHP5B4W

# Feature Flags
ENABLE_COMMUNITY_FEATURES=true
ENABLE_ANALYTICS=true
ENABLE_AI_ASSISTANT=false

# Logging
LOG_LEVEL=INFO
LOG_TO_STDOUT=true

# Content Security Policy
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC="'self' 'unsafe-inline' https://cdn.jsdelivr.net https://www.google-analytics.com"
CSP_STYLE_SRC="'self' 'unsafe-inline' https://cdn.jsdelivr.net"
CSP_IMG_SRC="'self' data: https:"
CSP_FONT_SRC="'self' https://cdn.jsdelivr.net"
CSP_CONNECT_SRC="'self'"
